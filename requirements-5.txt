# =====================================================================
# 📚 requirements.txt — بوت أركاديا (Arcadia Cloud Bot)
# مبني فعليًا على استيرادات main.py الحالية، مع دعم كل الميزات المدمجة:
# التحويل السحابي (CloudConvert/Convertio/ConvertAPI) والمحلي، السحب
# المتطور، الأخبار المتطورة، المسابقات، المحادثة الذكية (AI Talk)، الترجمة
# المزدوجة (ذكاء اصطناعي/محلي)، الفحص الأمني لنوع الملفات، الدعوة التلقائية،
# ونظام تقطيع الملفات الكبيرة والاستئناف (Upstash Redis + مراقبة رام).
# التثبيت: pip install -r requirements.txt
# =====================================================================

# ==================== 🔥 القسم الأساسي (إلزامي) ====================
pyTelegramBotAPI>=4.14.0          # إطار البوت الأساسي
requests>=2.31.0                  # طلبات HTTP (كل الـ APIs الخارجية)
psycopg2-binary>=2.9.9            # قاعدة البيانات PostgreSQL (Neon)
beautifulsoup4>=4.12.0            # استخراج HTML (السحب + الأخبار + SmartSearch)
wikipedia>=1.4.0                  # بحث ويكيبيديا (نظام المحادثة الذكية AI Talk)

# ==================== 📄 تحويل ودمج الملفات محليًا (fallback دائم) ====================
# تُستخدم كخط دفاع أخير حتى لو فشلت كل الخدمات السحابية أو لم تكن مفعّلة،
# وأيضًا محرك التقطيع/الدمج الأساسي لملفات PDF الكبيرة (pypdf).
python-docx>=1.1.0                # إنشاء/قراءة مستندات Word
pypdf>=4.0.0                      # دمج/تقسيم/معالجة PDF (يشمل نظام التقطيع)
reportlab>=4.0.0                  # إنشاء PDF من الصفر (يدعم العربي)
ebooklib>=0.18                    # إنشاء/قراءة كتب EPUB
arabic-reshaper>=3.0.0            # إعادة تشكيل الحروف العربية بالـ PDF
python-bidi>=0.4.2                # دعم الكتابة من اليمين لليسار (RTL)

# ==================== ☁️ التحويل السحابي (اختياري، مفعّل إن وُجد المفتاح) ====================
# سلسلة convert_file بالترتيب: CloudConvert (CLOUDCONVERT_API_KEY) → Convertio
# (CONVERTIO_API_KEY) → ConvertAPI (CONVERTAPI_SECRET) → التحويل المحلي أخيرًا.
# أي خطوة تفشل أو مفتاحها غائب تُتخطى تلقائيًا للتي بعدها.
cloudconvert>=3.0.2                # SDK رسمي لـ CloudConvert
# ملاحظة: Convertio وConvertAPI يُستخدمان عبر requests مباشرة (REST API بسيط)
# بدون حاجة SDK منفصل، لذا لا توجد مكتبة إضافية مطلوبة لهما هنا.

# ==================== 🌍 الترجمة المحلية (اختياري، بديل للذكاء الاصطناعي) ====================
# محرك ترجمة بديل يُفعَّل من لوحة التحكم (translation_settings.engine = "local").
# فوري ومجاني بدون رصيد OpenRouter، لكن أقرب للحرفية بدون صقل أدبي. الافتراضي
# يبقى "ai" (عبر q_tr/OpenRouter) — هذي المكتبات لا تُحمَّل بالذاكرة إلا لو
# فعّلت المحرك المحلي فعليًا (استيراد كسول).
deep-translator>=1.11.4           # محرك ترجمة Google بدون مفتاح API
langdetect>=1.0.9                 # كشف لغة النص تلقائيًا (مساعد لمحرك الترجمة المحلي)

# ==================== 🛡️ الأمان (اختياري، fail-open إن لم تُثبَّت) ====================
# فحص نوع الملف الحقيقي من محتواه الثنائي (بغض النظر عن الامتداد المكتوب)،
# يُشغَّل تلقائيًا عند كل رفع ملف لأداة التحويل. لو غير مثبتة، الفحص يُتخطى
# بأمان (fail-open) بدل ما يعطّل رفع الملفات بالكامل.
# على لينكس تحتاج أيضًا حزمة نظام: apt-get install libmagic1
python-magic>=0.4.27

# ==================== 🧩 التقطيع والاستئناف (اختياري، fallback تلقائي لـ PostgreSQL) ====================
# نظام تقطيع/دمج الملفات الكبيرة (chunking_settings.enabled). يخزّن حالة
# المهام بـ Upstash Redis (UPSTASH_REDIS_REST_URL + UPSTASH_REDIS_REST_TOKEN)
# لو مضبوطة، وإلا يرجع تلقائيًا لتخزينها بـ PostgreSQL الموجودة أصلاً —
# البوت لا يتعطل أبدًا بغياب Redis. psutil اختيارية لمراقبة الرام فقط؛
# بدونها المراقبة تُتخطى بأمان (fail-open) دون إيقاف عملية التقطيع.
upstash-redis>=1.1.0
psutil>=5.9.0

# =====================================================================
# 💡 ملاحظة: كل مكتبة أعلاه مستخدمة فعليًا في main.py الحالي بعد الدمج.
# لا توجد مكتبات "احتياطية للمستقبل" هنا عمدًا — أي مكتبة غير مستخدمة
# بالكود لا تُضاف، تجنبًا لتضخيم بيئة التثبيت بلا داعٍ على استضافة محدودة
# الموارد مثل Bot-Hosting.net.
# =====================================================================
